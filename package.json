{
  "name": "health-mcp",
  "version": "1.1.0",
  "description": "Self-hosted health-data ingest + MCP read server (steps / heart rate / sleep) for a Gadgetbridge fork.",
  "license": "MIT",
  "type": "commonjs",
  "main": "health-server.js",
  "scripts": {
    "start": "node health-server.js",
    "check": "node --check health-server.js && node --check shared/http-runtime.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "dotenv": "^16.4.7",
    "express": "^5.2.1",
    "zod": "3.25.76"
  }
}
